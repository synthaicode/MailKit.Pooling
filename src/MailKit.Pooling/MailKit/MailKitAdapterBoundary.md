# MailKit Adapter Boundary

This folder is reserved for the concrete MailKit adapter implementation.

The first implementation step should keep all direct MailKit references here so unit tests can exercise the pool core without the MailKit concrete client.
